import { GetMealRoutePipe } from './get-route.pipe';

describe('GetRoutePipe', () => {
  it('create an instance', () => {
    const pipe = new GetMealRoutePipe();
    expect(pipe).toBeTruthy();
  });
});
