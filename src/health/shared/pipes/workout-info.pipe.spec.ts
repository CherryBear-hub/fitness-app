import { WorkoutInfoPipe } from './workout-info.pipe';

describe('WorkoutInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new WorkoutInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
