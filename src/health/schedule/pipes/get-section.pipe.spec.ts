import { GetSectionPipe } from './get-section.pipe';

describe('GetSectionPipe', () => {
  it('create an instance', () => {
    const pipe = new GetSectionPipe();
    expect(pipe).toBeTruthy();
  });
});
